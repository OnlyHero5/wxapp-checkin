# 手机 Web 前端运行时路径配置示例
#
# 默认值对应当前仓库推荐的本地联调方式：
# - 前端开发服务器：`npm run dev`
# - 后端测试环境：`cd ../backend-rust && cargo run`
# - 后端默认监听：`http://127.0.0.1:9989`
# - 或使用一键脚本：在仓库根目录执行 `./scripts/dev.sh local`
VITE_APP_BASE_PATH=/
VITE_API_BASE_PATH=/api/web
VITE_API_PROXY_TARGET=http://127.0.0.1:9989

# 如果要和 `suda-gs-ams` 共域部署，建议改成下面这种子路径/独立 API 前缀：
# VITE_APP_BASE_PATH=/checkin/
# VITE_API_BASE_PATH=/checkin-api/web
