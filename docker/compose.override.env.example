# 把本文件复制为 `docker/compose.override.env`，仅在本机/服务器填写真实值。
# 仓库里的 `docker/compose.env` 只保证“一键能跑”，不适合作为最终生产密钥来源。

# ===== 建议至少替换下面这些值 =====
MYSQL_ROOT_PASSWORD=请改成真实强密码
MYSQL_PASSWORD=请改成真实强密码
DB_PASSWORD=请改成真实强密码
LEGACY_DB_PASSWORD=请改成真实强密码
QR_SIGNING_KEY=请替换为32字节以上强随机密钥

# ===== 如需接现网 legacy / Redis，可在这里覆盖 =====
# LEGACY_DB_URL=jdbc:mysql://legacy-db.example:3306/suda_union?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
# LEGACY_DB_USER=wxcheckin_app
# LEGACY_DB_PASSWORD=请填真实密码
# REDIS_HOST=redis.example.internal
# REDIS_PORT=6379
# REDIS_PASSWORD=请填真实密码
