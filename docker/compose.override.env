# 这是仓库内默认提供的 override 配置入口。
# 默认保持空值即可：此时系统会回退到 compose 内 demo `suda_union`，
# 并在启动日志里明确提示“当前是单项目演示状态，非生产在线状态”。
#
# 如需接外部 `suda_union`，只填写下面 3 项：
# - HOST：外部 legacy MySQL 的主机名 / IP；如需自定义端口，可写成 `host:port`
# - USER：访问 `suda_union` 的数据库账号
# - PASSWORD：对应数据库密码
#
# 示例：
# SUDA_UNION_DB_HOST=192.168.1.20
# SUDA_UNION_DB_HOST=192.168.1.20:3499
# SUDA_UNION_DB_USER=wxcheckin_app
# SUDA_UNION_DB_PASSWORD=请填真实数据库密码
#
# 维护提示：
# - 若你在本机改成真实值，请注意不要把真实密码再次提交回仓库。
# - 这 3 个值必须要么全空（演示模式），要么全部填写（外部 suda_union 模式）。
# 取消下面 3 行的注释后再填写真实值：
# SUDA_UNION_DB_HOST=
# SUDA_UNION_DB_USER=
# SUDA_UNION_DB_PASSWORD=
